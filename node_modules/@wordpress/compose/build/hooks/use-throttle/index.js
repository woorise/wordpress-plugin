"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = useThrottle;

var _lodash = require("lodash");

var _useMemoOne = require("use-memo-one");

var _element = require("@wordpress/element");

/**
 * External dependencies
 */

/**
 * WordPress dependencies
 */

/**
 * Throttles a function with Lodash's `throttle`. A new throttled function will
 * be returned and any scheduled calls cancelled if any of the arguments change,
 * including the function to throttle, so please wrap functions created on
 * render in components in `useCallback`.
 *
 * @param {...any} args Arguments passed to Lodash's `throttle`.
 *
 * @return {Function} Throttled function.
 */
function useThrottle() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }

  var throttled = (0, _useMemoOne.useMemoOne)(function () {
    return _lodash.throttle.apply(void 0, args);
  }, args);
  (0, _element.useEffect)(function () {
    return function () {
      return throttled.cancel();
    };
  }, [throttled]);
  return throttled;
}
//# sourceMappingURL=index.js.map