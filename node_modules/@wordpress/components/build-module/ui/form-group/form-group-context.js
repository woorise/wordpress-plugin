/**
 * WordPress dependencies
 */
import { createContext, useContext } from '@wordpress/element';
/**
 * @typedef {{ id?: import('react').ReactText, horizontal: boolean }} FormGroupContext
 */

/**
 * @type {FormGroupContext}
 */

var initialContext = {
  id: undefined,
  horizontal: true
};
/**
 * @type {import('react').Context<FormGroupContext>}
 */

export var FormGroupContext = createContext(initialContext);
export var useFormGroupContext = function useFormGroupContext() {
  return useContext(FormGroupContext);
};
/**
 * @param {string | undefined} id The preferred id for the form group element.
 * @return {import('react').ReactText | undefined} The form group context id.
 */

export var useFormGroupContextId = function useFormGroupContextId(id) {
  var contextId = useFormGroupContext().id;
  return id || contextId;
};
//# sourceMappingURL=form-group-context.js.map