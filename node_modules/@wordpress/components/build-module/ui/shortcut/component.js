import _extends from "@babel/runtime/helpers/esm/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import { createElement } from "@wordpress/element";

/**
 * External dependencies
 */
// eslint-disable-next-line no-restricted-imports
import { useContextSystem, contextConnect } from '@wp-g2/context';

function Shortcut(props, forwardedRef) {
  var _useContextSystem = useContextSystem(props, 'Shortcut'),
      shortcut = _useContextSystem.shortcut,
      className = _useContextSystem.className,
      otherProps = _objectWithoutProperties(_useContextSystem, ["shortcut", "className"]);

  if (!shortcut) {
    return null;
  }

  var displayText;
  var ariaLabel;

  if (typeof shortcut === 'string') {
    displayText = shortcut;
  } else {
    displayText = shortcut.display;
    ariaLabel = shortcut.ariaLabel;
  }

  return createElement("span", _extends({
    className: className,
    "aria-label": ariaLabel,
    ref: forwardedRef
  }, otherProps), displayText);
}

var ConnectedShortcut = contextConnect(Shortcut, 'Shortcut');
export default ConnectedShortcut;
//# sourceMappingURL=component.js.map