import { createElement, Fragment } from "@wordpress/element";

/**
 * External dependencies
 */
import { SafeAreaView } from 'react-native';
/**
 * WordPress dependencies
 */

import { Children } from '@wordpress/element';
import { createSlotFill, BottomSheetConsumer } from '@wordpress/components';

var _createSlotFill = createSlotFill('BottomSheetSubSheet'),
    Fill = _createSlotFill.Fill,
    Slot = _createSlotFill.Slot;

var BottomSheetSubSheet = function BottomSheetSubSheet(_ref) {
  var children = _ref.children,
      navigationButton = _ref.navigationButton,
      showSheet = _ref.showSheet,
      isFullScreen = _ref.isFullScreen;
  return createElement(Fragment, null, showSheet && createElement(Fill, null, createElement(SafeAreaView, null, createElement(BottomSheetConsumer, null, function (_ref2) {
    var setIsFullScreen = _ref2.setIsFullScreen;
    setIsFullScreen(isFullScreen);
    return children;
  }))), Children.count(children) > 0 && navigationButton);
};

BottomSheetSubSheet.Slot = Slot;
BottomSheetSubSheet.screenName = 'BottomSheetSubSheet';
export default BottomSheetSubSheet;
//# sourceMappingURL=index.native.js.map