"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.mergeValidationFunctions = mergeValidationFunctions;

/**
 * Merges validation functions together.
 *
 * @template {unknown[]} TArgs
 * @param {(...args: TArgs) => unknown} validate
 * @param {(...args: TArgs) => unknown} extraValidate
 *
 * @return {Function | undefined} A single merged validation function.
 */
function mergeValidationFunctions(validate, extraValidate) {
  if (typeof validate !== 'function') return;
  if (typeof extraValidate !== 'function') return validate;
  return function () {
    return validate.apply(void 0, arguments) || extraValidate.apply(void 0, arguments);
  };
}