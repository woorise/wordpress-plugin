"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.simpleEqual = simpleEqual;

/**
 * Quickly checks to see if two objects (shallow or deep) are equal.
 *
 * @param {any} objA
 * @param {any} objB
 *
 * @return {boolean} Whether the values are equal.
 */
function simpleEqual(objA, objB) {
  return objA === objB || JSON.stringify(objA) === JSON.stringify(objB);
}