"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isChrome = isChrome;
exports.isFirefox = isFirefox;
exports.isSafari = isSafari;

function detectBrowser(browser) {
  var _window, _window$navigator;

  return ((_window = window) == null ? void 0 : (_window$navigator = _window.navigator) == null ? void 0 : _window$navigator.userAgent.toLowerCase().indexOf(browser)) > -1;
}
/**
 * Check if the browser is Chrome.
 *
 * @return {boolean} True, if Chrome.
 */


function isChrome() {
  return detectBrowser('chrome');
}
/**
 * Check if the browser is Firefox.
 *
 * @return {boolean} True, if Firefox.
 */


function isFirefox() {
  return detectBrowser('firefox');
}
/**
 * Check if the browser is Safari.
 *
 * @return {boolean} True, if Safari.
 */


function isSafari() {
  return detectBrowser('safari');
}