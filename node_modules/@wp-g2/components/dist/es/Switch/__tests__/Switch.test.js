import { render } from '@testing-library/react';
import React from 'react';
import { Switch } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(Switch, {
      id: "switch"
    })),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render checked', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(Switch, {
      checked: true,
      id: "switch"
    })),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render disabled', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(Switch, {
      disabled: true,
      id: "switch"
    })),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render defaultValue', function () {
    var _render4 = render( /*#__PURE__*/React.createElement(Switch, {
      defaultValue: true,
      id: "switch"
    })),
        container = _render4.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render label', function () {
    var _render5 = render( /*#__PURE__*/React.createElement(Switch, {
      id: "switch",
      label: "Size"
    })),
        container = _render5.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render size', function () {
    var _render6 = render( /*#__PURE__*/React.createElement(Switch, {
      id: "switch",
      size: "small"
    })),
        container = _render6.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render type', function () {
    var _render7 = render( /*#__PURE__*/React.createElement(Switch, {
      id: "switch",
      type: "radio"
    })),
        container = _render7.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});