import { render } from '@testing-library/react';
import { noop } from '@wp-g2/utils';
import React from 'react';
import { Tag } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(Tag, null, "Status")),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render color', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(Tag, {
      color: "red"
    }, "Status")),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render display', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(Tag, {
      display: "block"
    }, "Status")),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render href', function () {
    var _render4 = render( /*#__PURE__*/React.createElement(Tag, {
      href: "#"
    }, "Status")),
        container = _render4.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render onRemove', function () {
    var _render5 = render( /*#__PURE__*/React.createElement(Tag, {
      onRemove: noop
    }, "Status")),
        container = _render5.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render removeButtonText', function () {
    var _render6 = render( /*#__PURE__*/React.createElement(Tag, {
      onRemove: noop,
      removeButtonText: "Remove"
    }, "Status")),
        container = _render6.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});