import { render } from '@testing-library/react';
import React from 'react';
import { Divider } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(Divider, null)),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render marginTop', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(Divider, {
      mt: 5
    })),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render marginBottom', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(Divider, {
      mb: 5
    })),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render margin', function () {
    var _render4 = render( /*#__PURE__*/React.createElement(Divider, {
      m: 7
    })),
        container = _render4.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});