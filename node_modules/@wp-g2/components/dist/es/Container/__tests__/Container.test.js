import { render } from '@testing-library/react';
import React from 'react';
import { Container } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(Container, null, "Some people are worth melting for.")),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render alignment', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(Container, {
      alignment: "left"
    }, "Some people are worth melting for.")),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render width', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(Container, {
      alignment: "left",
      width: 960
    }, "Some people are worth melting for.")),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});