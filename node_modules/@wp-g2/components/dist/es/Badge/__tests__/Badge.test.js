import { render } from '@testing-library/react';
import React from 'react';
import { Badge } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(Badge, null, "Status")),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render color', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(Badge, {
      color: "red"
    }, "Status")),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render display', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(Badge, {
      display: "block"
    }, "Status")),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render isBold', function () {
    var _render4 = render( /*#__PURE__*/React.createElement(Badge, {
      isBold: true
    }, "Status")),
        container = _render4.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render isRounded', function () {
    var _render5 = render( /*#__PURE__*/React.createElement(Badge, {
      isRounded: true
    }, "Status")),
        container = _render5.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render truncate', function () {
    var _render6 = render( /*#__PURE__*/React.createElement(Badge, {
      truncate: true
    }, "Status")),
        container = _render6.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});