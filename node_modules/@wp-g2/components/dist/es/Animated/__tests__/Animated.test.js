import { render } from '@testing-library/react';
import React from 'react';
import { Text } from '../../Text';
import { View } from '../../View';
import { Animated } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(Animated, null, "I like warm hugs")),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render as another HTML tag', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(Animated, {
      as: "span"
    }, /*#__PURE__*/React.createElement(Text, null, "I like warm hugs"))),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render as another Component', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(Animated, {
      as: View
    }, "I like warm hugs")),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render with auto animations', function () {
    var _render4 = render( /*#__PURE__*/React.createElement(Animated, {
      auto: true
    }, "I like warm hugs")),
        container = _render4.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});