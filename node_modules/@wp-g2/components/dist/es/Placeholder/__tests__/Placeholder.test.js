import { render } from '@testing-library/react';
import React from 'react';
import { Placeholder } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(Placeholder, null)),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render width', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(Placeholder, {
      width: 340
    })),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render height', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(Placeholder, {
      height: 340
    })),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});