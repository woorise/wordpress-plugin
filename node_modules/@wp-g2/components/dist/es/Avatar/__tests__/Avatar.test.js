import { render } from '@testing-library/react';
import React from 'react';
import { Avatar } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(Avatar, {
      name: "Elsa Oldenburg"
    })),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render with image src', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(Avatar, {
      name: "Elsa Oldenburg",
      src: "/elsa.png"
    })),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render with animateOnImageLoad disabled', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(Avatar, {
      animateOnImageLoad: false,
      name: "Elsa Oldenburg",
      src: "/elsa.png"
    })),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render with a border', function () {
    var _render4 = render( /*#__PURE__*/React.createElement(Avatar, {
      border: true,
      name: "Elsa Oldenburg",
      src: "/elsa.png"
    })),
        container = _render4.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render with a custom color', function () {
    var _render5 = render( /*#__PURE__*/React.createElement(Avatar, {
      border: true,
      color: "dodgerblue",
      name: "Elsa Oldenburg",
      src: "/elsa.png"
    })),
        container = _render5.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render with an alternate shape', function () {
    var _render6 = render( /*#__PURE__*/React.createElement(Avatar, {
      border: true,
      color: "dodgerblue",
      name: "Elsa Oldenburg",
      shape: "square",
      src: "/elsa.png"
    })),
        container = _render6.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render with an alternate size', function () {
    var _render7 = render( /*#__PURE__*/React.createElement(Avatar, {
      border: true,
      name: "Elsa Oldenburg",
      size: "large"
    })),
        container = _render7.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render with a custom size', function () {
    var _render8 = render( /*#__PURE__*/React.createElement(Avatar, {
      border: true,
      name: "Elsa Oldenburg",
      size: 41
    })),
        container = _render8.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render with a custom small size + square', function () {
    var _render9 = render( /*#__PURE__*/React.createElement(Avatar, {
      border: true,
      name: "Elsa Oldenburg",
      shape: "square",
      size: 20
    })),
        container = _render9.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render with a custom xSmall size + square', function () {
    var _render10 = render( /*#__PURE__*/React.createElement(Avatar, {
      border: true,
      name: "Elsa Oldenburg",
      shape: "square",
      size: 15
    })),
        container = _render10.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});