import { render } from '@testing-library/react';
import React from 'react';
import { Truncate } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(Truncate, null, "Some people are worth melting for.")),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render limit', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(Truncate, {
      limit: 5
    }, "Some people are worth melting for.")),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render custom ellipsis', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(Truncate, {
      ellipsis: "!!!",
      limit: 5
    }, "Some people are worth melting for.")),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render custom ellipsizeMode', function () {
    var _render4 = render( /*#__PURE__*/React.createElement(Truncate, {
      ellipsis: "!!!",
      ellipsizeMode: "middle",
      limit: 5
    }, "Some people are worth melting for.")),
        container = _render4.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render numberOfLines', function () {
    var _render5 = render( /*#__PURE__*/React.createElement(Truncate, {
      numberOfLines: 3
    }, "Some people are worth melting for.")),
        container = _render5.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});