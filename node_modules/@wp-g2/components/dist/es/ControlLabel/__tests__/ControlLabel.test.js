import { render } from '@testing-library/react';
import React from 'react';
import { ControlLabel } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(ControlLabel, null, "Label")),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render htmlFor', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(ControlLabel, {
      htmlFor: "Field"
    }, "Label")),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render size', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(ControlLabel, {
      size: "small"
    }, "Label")),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render no truncate', function () {
    var _render4 = render( /*#__PURE__*/React.createElement(ControlLabel, {
      truncate: false
    }, "Label")),
        container = _render4.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});