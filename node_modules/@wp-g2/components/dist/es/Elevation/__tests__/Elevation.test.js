import { render } from '@testing-library/react';
import React from 'react';
import { Elevation } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(Elevation, null)),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render isInteractive', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(Elevation, {
      isInteractive: true
    })),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render value', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(Elevation, {
      value: 7
    })),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render hover', function () {
    var _render4 = render( /*#__PURE__*/React.createElement(Elevation, {
      hover: 14,
      value: 7
    })),
        container = _render4.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render active', function () {
    var _render5 = render( /*#__PURE__*/React.createElement(Elevation, {
      active: 5,
      hover: 14,
      value: 7
    })),
        container = _render5.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render offset', function () {
    var _render6 = render( /*#__PURE__*/React.createElement(Elevation, {
      active: 5,
      hover: 14,
      offset: -2,
      value: 7
    })),
        container = _render6.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});