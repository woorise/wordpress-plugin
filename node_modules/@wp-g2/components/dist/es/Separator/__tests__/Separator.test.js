import { render } from '@testing-library/react';
import React from 'react';
import { Separator } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(Separator, null)),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render marginTop', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(Separator, {
      mt: 5
    })),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render marginBottom', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(Separator, {
      mb: 5
    })),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render margin', function () {
    var _render4 = render( /*#__PURE__*/React.createElement(Separator, {
      m: 7
    })),
        container = _render4.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});