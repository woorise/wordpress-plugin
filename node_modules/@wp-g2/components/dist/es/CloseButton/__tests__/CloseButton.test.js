import { render } from '@testing-library/react';
import React from 'react';
import { CloseButton } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(CloseButton, null)),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render variant', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(CloseButton, {
      variant: "primary"
    })),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render size', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(CloseButton, {
      size: "large"
    })),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});