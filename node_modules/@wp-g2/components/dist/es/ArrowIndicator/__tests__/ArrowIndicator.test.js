import { render } from '@testing-library/react';
import React from 'react';
import { ArrowIndicator } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(ArrowIndicator, null)),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render with a custom size', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(ArrowIndicator, {
      size: 17
    })),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});