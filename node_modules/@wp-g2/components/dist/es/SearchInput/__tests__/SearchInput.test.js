import { render } from '@testing-library/react';
import React from 'react';
import { SearchInput } from '../index';
describe('props', function () {
  test('should render correctly', function () {
    var _render = render( /*#__PURE__*/React.createElement(SearchInput, {
      id: "search"
    })),
        container = _render.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render isLoading', function () {
    var _render2 = render( /*#__PURE__*/React.createElement(SearchInput, {
      id: "search",
      isLoading: true
    })),
        container = _render2.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render placeholder', function () {
    var _render3 = render( /*#__PURE__*/React.createElement(SearchInput, {
      id: "search",
      placeholder: "Searching for..."
    })),
        container = _render3.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render prefix', function () {
    var _render4 = render( /*#__PURE__*/React.createElement(SearchInput, {
      id: "search",
      prefix: /*#__PURE__*/React.createElement("span", null, "prefix")
    })),
        container = _render4.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render suffix', function () {
    var _render5 = render( /*#__PURE__*/React.createElement(SearchInput, {
      id: "search",
      suffix: /*#__PURE__*/React.createElement("span", null, "suffix")
    })),
        container = _render5.container;

    expect(container.firstChild).toMatchSnapshot();
  });
  test('should render value', function () {
    var _render6 = render( /*#__PURE__*/React.createElement(SearchInput, {
      id: "search",
      value: "q"
    })),
        container = _render6.container;

    expect(container.firstChild).toMatchSnapshot();
  });
});