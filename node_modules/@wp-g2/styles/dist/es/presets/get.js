import { get } from '../core';
export { get } from '../core';
export { createToken } from '../system';
export function getTokenValue(token) {
  var cssVariable = get(token).replace('var(', '').replace(/\)$/g, '');
  var rawValue = window.getComputedStyle(document.documentElement).getPropertyValue(cssVariable);
  return rawValue;
}