"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toPx = toPx;

var _utils = require("@wp-g2/utils");

/**
 * @param {string | number} value
 * @return {string | number}
 */
function toPx(value) {
  if (_utils.is.numeric(value)) {
    return value + "px";
  }

  return value;
}