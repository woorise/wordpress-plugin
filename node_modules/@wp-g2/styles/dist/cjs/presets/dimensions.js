"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.frame = void 0;

var _utils = require("@wp-g2/utils");

var _styleSystem = require("../style-system");

/**
 * @param {{ height: import('create-emotion').ObjectInterpolation<any>['height'], width: import('create-emotion').ObjectInterpolation<any>['width'] }} options
 */
var frame = function (_ref) {
  var height = _ref.height,
      width = _ref.width;
  var styles = [];

  if (_utils.is.defined(width)) {
    styles.push([(0, _styleSystem.css)({
      maxWidth: '100%',
      width: width
    })]);
  }

  if (_utils.is.defined(height)) {
    styles.push([(0, _styleSystem.css)({
      height: height,
      maxHeight: '100%'
    })]);
  }

  return (0, _styleSystem.css)(styles);
};

exports.frame = frame;

frame.width = function (
/** @type {import('create-emotion').ObjectInterpolation<any>['width']} */
width) {
  return (0, _styleSystem.css)({
    maxWidth: '100%',
    width: width
  });
};

frame.height = function (
/** @type {import('create-emotion').ObjectInterpolation<any>['height']} */
height) {
  return (0, _styleSystem.css)({
    height: height,
    maxHeight: '100%'
  });
};